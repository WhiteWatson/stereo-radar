{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "前端监听事件 + 调用控制命令所需的默认权限",
  "windows": ["main"],
  "permissions": ["core:default", "core:event:default"]
}
