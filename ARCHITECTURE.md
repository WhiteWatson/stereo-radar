# Architecture

本文件描述 **stereo-radar 的系统架构**：组件如何划分、边界在哪、依赖朝哪个方向、数据如何流动、线程如何协作、在哪里扩展。

产品目标、设计决策理由、实施路线与风险见 [design.md](./design.md)，本文不重复。

---

## 1. 架构总览

系统是一条**单向数据流管线**：音频帧从采集端进入，逐级变换，最终成为屏幕上的方位光点。

```
┌──────────────┐   AudioFrame   ┌──────────┐  SourcePoint[]  ┌─────────────┐   event   ┌──────────┐
│  Capture     │ ─────────────▶ │   DSP    │ ──────────────▶ │   Bridge    │ ────────▶ │  UI      │
│ (source)     │   (push)       │ (transform)               │ (Tauri)     │  (IPC)    │ (webview)│
└──────────────┘                └──────────┘                 └─────────────┘           └──────────┘
   平台相关                        平台无关                      平台外壳                 平台无关
```

四个职责清晰、可独立替换的部分：

| 组件 | crate / 位置 | 关注点 | 平台相关性 |
|------|-------------|--------|-----------|
| **Capture** | `crates/core::capture` | 从某个音频来源产出 `AudioFrame` | **是**（封装于此） |
| **DSP** | `crates/core::dsp` | 把 `AudioFrame` 变换为 `SourcePoint[]` | 否（纯计算） |
| **Bridge** | `src-tauri` | 编排管线、管理窗口、把数据推给 UI | 外壳（Tauri） |
| **UI** | `src/` | 渲染 360° 雷达 | 否（Web） |

---

## 2. 依赖方向（最重要的架构约束）

依赖只能**从外向内**指向核心，核心不知道外部存在：

```
        ┌─────────────────────────────────────────┐
        │                src-tauri                 │  依赖 core，编排一切
        │   ┌───────────────────────────────────┐ │
        │   │              core (crate)         │ │  不依赖 tauri / 不依赖 UI
        │   │   capture  ──uses──▶  model       │ │
        │   │      dsp   ──uses──▶  model       │ │
        │   └───────────────────────────────────┘ │
        └─────────────────────────────────────────┘
                          ▲
              src/ (UI)   │  只通过事件/IPC 契约依赖，不依赖 Rust 内部类型
```

**规则：**
- `core` crate **不引用** Tauri、不引用任何 UI 概念。它是一个能独立编译、独立测试的纯 Rust 库。
- `capture` 和 `dsp` 互不依赖，只共享 `model`（数据类型）。DSP 不知道音频从哪来，Capture 不知道音频去哪。
- UI 与后端之间**唯一的耦合是数据契约**（见 §5），不共享实现类型。

这条约束保证：换采集来源、换 UI 框架、把 core 拿去做 CLI 版，都不会牵动其他层。

---

## 3. 组件边界

### 3.1 Capture —— 把"来源差异"关进盒子

所有平台/来源的差异都收敛在一个 trait 后面，上游只见接口：

```
AudioCapture (trait)
├── CpalCapture     跨平台，读虚拟声卡输入端（主力）
├── FileCapture     跨平台，循环读 8 声道 WAV（开发 / 测试）
└── WasapiCapture   Windows 兜底（可选）
```

- 边界对象：`AudioFrame`（多声道 PCM，planar `f32`）。
- 采集以 **push 模型**工作：底层音频回调驱动，每帧主动调用上游给的回调，而非上游轮询。
- 这是系统里**唯一**允许出现 `#[cfg(target_os)]` 的地方。其它层一律平台无关。

### 3.2 DSP —— 无状态变换 + 少量跨帧状态

- 输入 `AudioFrame`，输出 `SourcePoint[]`。
- 核心是纯函数式的逐帧变换（RMS → 向量合成）。
- 唯一的可变状态是**平滑/跟踪器**（EMA、峰值保持、音源 id 跨帧关联），封装在一个 `Analyzer` 结构里，生命周期由调用方持有。
- 不做 IO、不开线程、不感知时间来源 —— 便于用固定 WAV 做确定性单元测试。

### 3.3 Bridge —— 唯一的"编排者"

`src-tauri` 是把各部件接起来的地方，也是唯一有副作用编排的地方：
- 构造某个 `AudioCapture` 实现 + 一个 `Analyzer`。
- 在采集回调里跑 DSP，把结果送过线程边界（见 §4）。
- 配置窗口形态（透明 / 置顶 / click-through / 无边框）。
- 把 `SourcePoint[]` 作为事件发给 webview。

### 3.4 UI —— 纯渲染消费者

- 订阅后端事件，拿到 `FramePayload` 就渲染，不回头查询、不持有业务逻辑。
- 控制项（灵敏度/平滑/门限/设备选择）通过命令（command）反向调用后端，是一条独立的低频控制通道。

---

## 4. 线程与并发模型

管线跨越三类执行上下文，数据**单向穿过线程边界**：

```
[音频回调线程]            [桥接/DSP]                [Tauri 主线程]        [WebView 线程]
 OS 音频驱动 ──frame──▶  Analyzer.process ──SourcePoint[]──▶ emit event ──IPC──▶ render
   实时、不可阻塞          轻量、可在回调内              托管窗口/事件循环        requestAnimationFrame
```

**约束与策略：**
- **音频回调线程是实时线程**：绝不允许阻塞操作（锁竞争、分配、IO、日志）。DSP 必须足够轻量以在回调内完成；否则改为"回调只搬运到无锁环形缓冲，DSP 在独立工作线程消费"。
- **跨线程传递**用有界 channel / 无锁队列，**有背压**：UI 渲染（30–60fps）慢于音频帧时，丢弃旧帧而非堆积，保证低延迟与有限内存。
- **控制通道与数据通道分离**：UI→后端的参数调整是低频、可加锁的独立路径，不进实时音频线程的热路。
- DSP 的跨帧状态（`Analyzer`）**单线程拥有**，不共享可变状态，避免锁。

---

## 5. 跨边界契约

两处契约定义了组件间的"接缝"，是最该保持稳定的部分。

### 5.1 内部边界对象（Rust，`core::model`）
- `AudioFrame`：capture → dsp 之间。声道数、采样率、planar 数据。
- `SourcePoint` / `FramePayload`：dsp → bridge → UI 之间。方位角、强度、音源 id、（可选）频段、调试用声道能量。

### 5.2 后端 ↔ 前端契约（IPC）
- **数据通道（后端 → 前端）**：事件 `sources`，payload = `FramePayload`（结构见 design.md §7）。高频、单向。
- **控制通道（前端 → 后端）**：Tauri command，如 `set_params`、`list_devices`、`select_device`。低频、请求/响应。

契约用普通可序列化结构表达，**不泄露任一侧的内部实现类型**，因此前端框架或后端实现可独立演进。

---

## 6. 配置与生命周期

- **启动**：Bridge 读取/初始化参数 → 枚举设备 → 选定 `AudioCapture` 实现并 `start` → 进入推流。
- **运行期重配**（换设备、改灵敏度）：通过控制通道更新；换设备 = `stop` 旧采集、`start` 新采集，DSP 的 `Analyzer` 可保留或重置。
- **关闭**：`stop` 采集 → 释放音频资源 → 关闭窗口。
- 采集来源、DSP 参数等通过构造期注入，便于在测试中替换为 `FileCapture` + 固定参数。

---

## 7. 扩展点

架构预留了几处"加东西不动主干"的位置：

| 想加什么 | 在哪扩展 | 是否影响其它层 |
|----------|---------|--------------|
| 新的音频来源（新虚拟声卡、网络流、回放文件） | 新增 `AudioCapture` 实现 | 否 |
| 新的方位算法 / 频段分离 / 多源聚类 | 在 `dsp` 内增强 `Analyzer`，契约不变 | 否（`SourcePoint` 稳定即可） |
| 新的可视化（极简光点 / 拟物雷达 / 外部 LED 硬件） | 新增事件消费者，订阅同一契约 | 否 |
| 平台扩展（Linux 等） | 新增平台相关 `AudioCapture` 实现 | 否 |
| 无头/CLI 模式 | 直接复用 `core`，跳过 Tauri/UI | 否 |

判断扩展是否"健康"的标准：**只要不改 §5 的契约和 §2 的依赖方向，就是局部改动。**

---

## 8. 测试架构

依赖注入 + 平台无关核心使得大部分逻辑可在 Mac 上确定性测试：

- **DSP 单元测试**：`FileCapture` 喂固定的"声源绕圈" 8 声道 WAV → 断言输出的 `angle` 序列符合预期。无需真实设备、无需真游戏。
- **核心库独立**：`core` 不依赖 Tauri，可单独 `cargo test`。
- **UI 与算法解耦**：前端可用 mock 的 `FramePayload` 流独立开发渲染。

---

## 9. 一句话总结

> 一条**单向、平台无关核心 + 可替换边缘**的音频可视化管线：差异锁在 Capture 的 trait 后，逻辑集中在无状态的 DSP，外壳（Tauri）只负责编排与窗口，UI 只消费契约。换来源、换算法、换前端、上 CLI，都是不动主干的局部操作。
